package com.ghostcorp.designpatterns

object SingletonPattern {

    // Singleton pattern in which there is a object whose only a single instance exits in your entire codebase

    // In kotlin we use object key word to make a singleton

    fun doSomething() {

    }
}